import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  // Gets rendered to the <div> ngen-cal-react
  document.getElementById('ngen-cal-react')
);
